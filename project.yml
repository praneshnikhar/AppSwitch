name: MacDeck
options:
  bundleIdPrefix: com.macdeck
  deploymentTarget:
    macOS: "14.0"
    iOS: "17.0"
settings:
  SWIFT_VERSION: "5.9"
targets:
  MacApp:
    type: application
    platform: macOS
    sources:
      - MacApp
      - Shared
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.macdeck.mac
      INFOPLIST_FILE: MacApp/Info.plist
    info:
      path: MacApp/Info.plist
      properties:
        CFBundleName: Mac Deck
        CFBundleDisplayName: Mac Deck
        CFBundlePackageType: APPL
        LSMinimumSystemVersion: "14.0"
  iOSApp:
    type: application
    platform: iOS
    sources:
      - iOSApp
      - Shared
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.macdeck.ios
      INFOPLIST_FILE: iOSApp/Info.plist
      TARGETED_DEVICE_FAMILY: "1"
    info:
      path: iOSApp/Info.plist
      properties:
        CFBundleName: Mac Deck
        CFBundleDisplayName: Mac Deck
        CFBundlePackageType: APPL
        LSRequiresIPhoneOS: true
        UILaunchStoryboardName: LaunchScreen
        NSLocalNetworkUsageDescription: Mac Deck needs local network access to discover your Mac.
        NSBonjourServices:
          - _macdeck._tcp
